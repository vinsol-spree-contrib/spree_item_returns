Hi Admin

We have received a return request against the order <%= @return_authorization.order.number %>.

<table>
  <th>
    <td>Product</td>
    <td> Quantity</td>
    <td>Price</td>
  </th>
  <% @return_authorization.return_items.each do |return_item| %>
  <tr> <%= return_item.variant.name %> </tr>
  <tr> 1 </tr>
  <tr> $ <%= return_item.variant.price.to_f %> </tr>
  <% end %>
</table>

Reason: <%= @return_authorization.reason.name %>
Please review it by following the link here
<%= link_to 'Request', spree.edit_admin_order_return_authorization_url(@return_authorization.order, @return_authorization) %>.

Thank you
